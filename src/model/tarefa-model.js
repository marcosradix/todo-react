export class Tarefa{

  constructor(categoria,descricao, done, email, id) {
    this.categoria = categoria;
    this.descricao = descricao;
    this.done = done;
    this.email = email;
    this.id = id;
  }
}
